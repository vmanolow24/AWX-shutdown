---
- name: Stop K3s completely
  hosts: all
  become: true
  tasks:
    - name: Stop K3s server if exists
      ansible.builtin.systemd:
        name: k3s
        state: stopped
        enabled: no
      failed_when: false
      ignore_errors: yes



